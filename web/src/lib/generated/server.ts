// Code generated by tygo. DO NOT EDIT.
import type { Config } from "./config";
import type { SnapshotConn } from "./core";

//////////
// source: wire.go

/**
 * Envelope is the single wire format for all WebSocket traffic.
 * Message types are dot-namespaced, see docs/protocols.md.
 */
export interface Envelope {
  type: string;
  ts: string /* RFC3339 */;
  data: any;
}
/**
 * NICInfo describes one local network interface — used to bind outputs to a
 * specific NIC on multi-homed machines (GET /api/system/interfaces).
 */
export interface NICInfo {
  name: string;
  mac: string;
  mtu: number /* int */;
  up: boolean;
  multicast: boolean;
  loopback: boolean;
  ipv4: string[]; // CIDR strings, e.g. "192.168.1.5/24"
}
/**
 * Snapshot is the payload of the initial "state.snapshot" WS message.
 */
export interface Snapshot {
  version: string;
  commit: string;
  connectors: SnapshotConn[];
  config: Config;
}
